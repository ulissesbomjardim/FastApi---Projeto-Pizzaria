# 🔐 Variáveis de Ambiente - Hashtag Pizzaria
# Copie este arquivo para .env e ajuste os valores conforme necessário

# === SEGURANÇA ===
# Chave secreta para JWT (OBRIGATÓRIO: gere uma chave segura para produção)
SECRET_KEY=seu_secret_key_super_seguro_aqui_com_mais_de_32_caracteres_exemplo

# === POSTGRESQL (DOCKER) ===
# Configurações do PostgreSQL (usadas pelo Docker Compose)
POSTGRES_DB=pizzaria_db
POSTGRES_USER=pizzaria_user
POSTGRES_PASSWORD=sua_senha_postgres_segura

# === DATABASE ===
# URL de conexão com o banco (Docker: usar 'postgres', Local: usar 'localhost')
DATABASE_URL=postgresql://pizzaria_user:sua_senha_postgres_segura@postgres:5432/pizzaria_db

# === APLICAÇÃO ===
# Configurações gerais da aplicação
DEBUG=True
ENVIRONMENT=development

# === USUÁRIOS PADRÃO ===
# Usuário administrador (criado automaticamente)
ADMIN_EMAIL=admin@pizzaria.com
ADMIN_PASSWORD=Admin123!@#

# Usuário de teste (criado automaticamente)
EMAIL=teste1@example.com
PASSWORD=Minh@Senha1

# === CONFIGURAÇÕES OPCIONAIS ===
# Configurações de CORS (se necessário)
# ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Configurações de email (se implementar)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=seu_email@gmail.com
# SMTP_PASSWORD=sua_senha_app

# === NOTAS IMPORTANTES ===
# 1. NUNCA commite o arquivo .env real no Git!
# 2. Para produção, altere todas as senhas e chaves
# 3. Use HTTPS em produção
# 4. Configure CORS adequadamente para seu domínio